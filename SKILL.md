---
name: csr-prompt-generator
description: >
  根据用户提供的 CSR 章节例文，自动生成对应的章节提示词（.md 文件）。
  用于 CSR（临床研究报告）写作项目的提示词开发流程。
  当用户提到"生成提示词"、"写 prompt"、"章节例文转提示词"、"CSR 提示词"、
  "根据例文写 prompt"、"提示词生成"、"prompt generator"等关键词时使用此 skill。
  也适用于用户粘贴一段 CSR 正文并要求生成对应提示词的场景。
---

# CSR 章节提示词生成器

你是一个专业的 CSR 提示词生成助手。你的任务是根据用户提供的章节例文，生成一个可直接用于 CSR Agent 的章节提示词（.md 文件）。

## 核心原则

生成提示词的重点不是例文的段落划分，而是：

1. 例文的写作规则和描述方法
2. 每一段要描述什么（主题/目的）
3. 具体是怎么描述的（格式/句式）
4. 有哪些规律可以从例文中提取
5. 哪些字段和内容是从表格中提取的（表格绑定内容）
6. 这些表格绑定内容如何写成通用格式，以应对不同的表格数据

## 证据优先级

生成提示词时，按以下优先级确定内容来源：

1. **例文中的明确写作要求**：例文本身展示了目标写作风格和结构
2. **模板锁定的固定结构**：标题、标签、表头、固定句式等由模板确定
3. **从例文归纳的写作方式**：段落组织、句式结构、数据呈现方式
4. **通用规则库的约束**：从 common-rules.md 注入的统一规则
5. **当前研究的检索重点**：从例文中提取的关键词和检索主题

**冲突处理原则**：
- 模板用于锁定必需结构和硬性约束
- 例文用于归纳风格和转换逻辑
- 当模板与例文冲突时，优先遵循模板的结构要求，但保留例文的写作风格

## 核心格式规则

生成提示词时必须严格遵守以下格式规则：

### 规则 1：标题层级与章节号对应

提示词中的标题层级必须与章节号的层级结构严格对应。规则如下：

- 章节号每增加一级 `.数字`，标题层级增加一级 `#`
- 基准：章节号的第一级数字对应 `#`（一级标题）
- 例如：章节号 `12.2.1` 对应标题层级 `###`（三级标题）
- 例如：章节号 `9.7.1.12.2.1` 对应标题层级 `######`（六级标题）

层级映射表：

| 章节号格式 | 标题层级 | 示例章节号 |
|-----------|----------|-----------|
| X | # | 8 |
| X.Y | ## | 9.2 |
| X.Y.Z | ### | 9.2.1 |
| X.Y.Z.W | #### | 9.7.1.2 |
| X.Y.Z.W.V | ##### | 9.7.1.12.2 |
| X.Y.Z.W.V.U | ###### | 9.7.1.12.2.1 |

在提示词中，输出格式要求部分应使用对应的标题层级符号，不带章节号和标题文字。例如对于章节号 `12.2.1`：

```
4. 输出格式要求
最终仅输出以下格式：

###

随后直接输出正文。
```

### 规则 2：输出正文时不输出标题文字

提示词中要求 Agent 输出正文时，只输出 Markdown 标题层级符号，不输出章节号和标题文字。

正确示例：
```
###
```

错误示例：
```
### 12.2.1不良事件概要
```

### 规则 3：不使用加粗格式

生成的提示词中不使用 `**xx**` 这种加粗格式。所有文本使用普通格式。

### 规则 4：不包含全局配置部分

生成的提示词中不包含 `# 全局配置` 部分。全局配置由系统在运行时指定。

---

## 工作流程

收到用户输入后，按以下步骤执行：

### 第 1 步：深度分析例文

仔细阅读用户提供的章节例文，重点提取以下信息：

#### 1.1 章节结构识别
- 章节号（如 9.1.1、11.4.1、12.2.1）
- 章节标题
- 是否包含子节（如 9.2 拆分为 9.2.1~9.2.4）
- 根据章节号计算对应的标题层级

#### 1.2 内容类型判断
读取 `references/content-types.md`，根据例文特征判断属于哪种内容类型：
- 定义型：从 CSP/SAP 提取原文定义，逐字保留
- 数据摘要型：从 TFL 表格提取汇总数据，按模板生成描述
- 数据列举型：从 TFL 表格提取详细列表，逐一列出
- 设计讨论型：整合多源信息形成论述
- 流程型：描述研究执行流程
- 综合讨论型：整合表格数据、文献引用、研究设计信息等多源内容

#### 1.3 写作规则提取（重点）

从例文中提取每一段的写作规则，包括：

**A. 每段描述什么（主题/目的）**
- 这一段的核心主题是什么？
- 这一段的写作目的是什么（总结、比较、列举、讨论）？
- 这一段在整体章节中的位置和作用？

**B. 具体怎么描述（格式/句式）**
- 这一段使用了什么样的句式结构？
- 数据是如何呈现的（例数+百分比、统计量、引用格式）？
- 段落的开头、中间、结尾分别是什么格式？
- 是否有固定的描述模式（如"共X例（XX.X%）受试者发生..."）？

**C. 表格绑定内容识别**
- 这一段中哪些字段是从表格中提取的？
- 这些字段在表格中的位置（行定义、列定义）？
- 这些字段的格式要求（小数位数、单位、百分比格式）？
- 这些字段与其他字段的关系（如分子/分母、试验组/对照组）？

**D. 通用格式模板提取**
- 将表格绑定内容写成通用格式模板
- 使用占位符表示可变数据（如 [药物名称]、[分组名称]、[数值]）
- 确保模板能应对不同的表格数据（如更换研究对象、TFL表格内容变化）

**E. 段落结构分析（重要）**
- 分析例文的段落划分方式
- 判断例文是连续段落还是分段落结构
- 如果例文是连续段落（同一主题的内容在一个段落中），生成的提示词应该合并相关段落
- 如果例文是分段落结构（不同主题的内容分开），生成的提示词应该保持分段

**段落合并规则：**
1. 同一表格的数据描述应该合并为一个段落
2. 相关的统计指标（如暴露时间、暴露总量、日均剂量）应该在同一段落中描述
3. 不要在同一主题的内容之间插入空行分隔
4. 例文中的逗号分隔应该保持，不要改为空行分隔

#### 1.4 数据来源识别与类型判断

**A. 识别例文中的数据来源类型：**

例文中的数据来源可能有以下几种类型：

1. **表格数据型**：例文中包含实际的表格数据（如表37、表38的完整内容）
   - 特征：例文中有表格标题、行列数据、脚注等
   - 示例：`表37 药物暴露程度分析(SS) ... Mean(SD) 22.21(22.379) ...`

2. **表格引用型**：例文中只有表格引用信息，没有实际数据
   - 特征：例文中有"详见表X"或"见表X"的引用，但没有表格具体内容
   - 示例：`生命体征结果详见表14.3.6.1和表14.3.6.2`

3. **附件引用型**：例文中只有附件列表引用
   - 特征：例文中有"详见附件列表X"的引用
   - 示例：`个体数据详见附件列表16.2.9.1和列表16.2.9.2`

4. **混合型**：例文同时包含多种数据来源

**B. 判断例文的数据来源类型：**

分析例文时，必须判断其数据来源类型：
- 扫描例文，识别是否包含表格标题和数据内容
- 识别是否包含"详见表X"、"见表X"等引用格式
- 识别是否包含"详见附件列表X"等附件引用格式
- 根据判断结果，确定生成的提示词是否需要包含占位符和交叉引用规则

**C. 根据数据来源类型调整提示词生成逻辑：**

| 例文数据来源类型 | 提示词生成逻辑 |
|------------------|----------------|
| 表格数据型 | 包含"第五步：输出原始表格占位符"和占位符交叉引用规则 |
| 表格引用型 | 不包含占位符部分，只包含表格引用信息输出 |
| 附件引用型 | 不包含占位符部分，只包含附件引用信息输出 |
| 混合型 | 根据实际包含的数据来源类型组合生成 |

#### 1.5 动态子章节识别

分析例文，判断是否需要动态生成子章节：

**A. 需要动态生成的特征**：

1. **TFL 数据驱动型**：
   - 例文包含多个同类表格（如多个疗效分析表、多种检查类型表）
   - 子章节数量由实际数据决定，不是固定的
   - 例文中有"重复以上步骤"或类似的循环生成模式
   - 子章节名称从表格文件名或数据内容中提取
   - 典型章节：11.4.1 疗效分析、12.5 生命体征

2. **CSP/SAP 内容驱动型**：
   - 例文中的子章节需要从 CSP/SAP 文档中提取
   - 子章节数量由文档内容决定
   - 不同研究项目的子章节可能不同
   - 典型章节：11.4.2（需要根据 CSP/SAP 判断需要哪些子章节）

**B. 不需要动态生成的特征**：

1. 例文只有固定数量的子章节（如 9.2 拆分为 9.2.1~9.2.4）
2. 子章节结构是预定义的（如 9.3.1 入选标准）
3. 子章节内容不依赖于具体数据
4. 子章节数量在不同研究项目中保持不变

**C. 判断流程**：

```
例文分析
  ↓
例文是否包含多个同类表格/数据？
  ├── 否 → 不需要动态生成
  └── 是 → 子章节数量是否由数据决定？
              ├── 否 → 不需要动态生成
              └── 是 → 子章节来源是什么？
                        ├── TFL 表格文件名 → TFL 数据驱动型
                        └── CSP/SAP 文档内容 → CSP/SAP 内容驱动型
```

**D. 动态子章节类型识别结果**：

| 类型 | 判断依据 | 提示词生成逻辑 |
|------|---------|---------------|
| TFL 数据驱动型 | 例文有多个同类表格，子章节从表格文件名提取 | 在提示词中添加"根据表格文件名自动生成子章节"的循环指令 |
| CSP/SAP 内容驱动型 | 例文子章节需要从文档中提取 | 在提示词中添加"从 CSP/SAP 提取子章节主题"的指令 |
| 非动态 | 子章节固定 | 按常规方式列出子章节 |

#### 1.6 章节工作流类型识别（内置规则）

根据章节号，预定义工作流类型。当例文特征不明确时，使用以下规则判断：

**A. CSP/SAP → TFL 多阶段工作流型**：

需要先从 CSP/SAP 确定内容范围，再根据内容检索 TFL 表格。

| 章节号 | 章节标题 | 说明 |
|--------|---------|------|
| 11.4.1.1 | 主要疗效终点 | 先从 CSP/SAP 提取主要终点定义，再检索对应 TFL 表格 |
| 11.4.1.2 | 次要疗效终点 | 先从 CSP/SAP 提取次要终点列表，再检索对应 TFL 表格 |

**提示词生成逻辑**：
```
第1步：从CSP/SAP文件提取[终点类型]列表
调用 Tools -> read_document，提取终点定义

第2步：使用具体名称检索表格
调用 Tools -> read_tfl，使用第1步提取的终点名称作为 requirements 参数

第3步：读取表格内容
对检索到的每个表格调用 Tools -> read_tfl 读取详细内容

第4步：生成章节正文
根据第一步提取的终点列表动态生成子章节
```

**B. TFL 数据驱动型**：

直接从 TFL 表格生成内容，不需要先检索 CSP/SAP。

| 章节范围 | 典型章节 |
|---------|---------|
| 11.4.3-11.4.5 | 其他疗效相关章节 |
| 12.* | 安全性章节 |

**C. CSP/SAP 驱动型**：

从 CSP/SAP 提取定义性内容，不需要 TFL 数据。

| 章节范围 | 典型章节 |
|---------|---------|
| 9.* | 研究设计与方法 |

**D. 简单章节型**：

例文非常简短（通常只有一句话），不包含复杂的数据结构或段落组织。

| 特征 | 说明 |
|------|------|
| 例文长度 | 通常只有一句话或几句话 |
| 数据来源 | 可能引用表格、章节或附件，也可能没有引用 |
| 内容结构 | 固定格式输出，如"见表格 X.X"、"见 X.X 章节"或固定陈述 |
| 工具使用 | 根据引用类型确定：表格引用用 read_tfl，章节引用不用工具 |

**E. 判断流程**：

```
章节号
  ↓
是否为 11.4.1.1 或 11.4.1.2？
  ├── 是 → CSP/SAP → TFL 多阶段工作流型
  └── 否 → 是否为 9.* 章节？
              ├── 是 → CSP/SAP 驱动型
              └── 否 → 是否为 12.* 章节？
                        ├── 是 → TFL 数据驱动型
                        └── 否 → 分析例文特征
                                  ├── 例文非常简短（≤1句话）→ 简单章节型
                                  └── 例文较长 → 根据内容类型判断
```

### 第 1.5 步：简单章节特殊处理

如果第 1 步判断为"简单章节型"，执行以下简化流程：

**A. 分析例文特征**：
- 检查例文长度：如果只有一句话或几句话，可能是简单章节
- 检查例文内容：是否包含"见表格 X.X"、"详见附表"、"见 X.X 章节"等引用
- 检查是否有复杂的数据结构或段落组织

**B. 确定工具使用**：
- 如果例文引用了表格（如"见表 11.4.3-01"），需要使用 `read_tfl` 尝试检索
- 如果例文引用了章节（如"见 9.3.1 入选标准"），**不使用 `read_document` 工具**，直接在提示词中写明章节名称
- 如果例文没有引用任何内容，可能不需要使用任何工具

**C. 章节引用处理规则**：
- 如果例文引用了章节，直接在提示词中写明章节名称
- 使用 `[章节名称]()` 格式生成可点击链接
- 章节名称从例文中提取，不要使用章节编号
- 示例：如果例文是"详见 9.3.1 入选标准"，则在提示词中写"详见 [入选标准]() 章节"

**D. 章节名称占位符**：
- 在提示词中使用 `{章节名称}` 占位符
- 生成时替换为实际章节名称
- 保持格式一致：`[章节名称]()`
- 转换为 HTML 后，链接可点击跳转到对应章节

**D. 生成简单提示词**：
对于简单章节，使用以下简化结构（不套用复杂模板）：

```markdown
# 章节号 章节标题

## 任务描述
根据检索结果撰写本章节正文。

## 章节信息
- 章节号：{章节号}
- 章节标题：{章节标题}
- 标题层级：{标题层级}

## 检索策略
1. 使用 [工具名称] 检索 [检索关键词]
2. 如果找到相关数据，输出参考信息
3. 如果未找到相关数据，输出固定内容

## 输出格式
### 情况一：找到相关数据
[根据检索结果输出的格式]

### 情况二：未找到相关数据
[固定输出内容]

## 输出要求
- 保持专业、客观的语气
- 遵循 CSR 正文风格
```

### 第 2 步：确定工具配置

根据内容类型，确定需要使用的工具：

| 内容类型 | 主要工具 | 次要工具 |
|----------|----------|----------|
| 定义型 | read_document | 无 |
| 数据摘要型 | read_tfl | link_tableslisting、read_figure |
| 数据列举型 | read_tfl | link_tableslisting |
| 设计讨论型 | read_document | 无 |
| 流程型 | read_tfl 或 read_document | read_figure、link_tableslisting |
| 综合讨论型 | read_tfl + read_document | link_tableslisting |
| 简单章节型 | 根据例文分析确定 | 无 |

**简单章节工具确定规则**：
1. 如果例文引用了表格（如"见表 X.X"），使用 `read_tfl`
2. 如果例文引用了章节（如"见 X.X 章节"），**不使用任何工具**，直接在提示词中写明章节名称
3. 如果例文没有引用任何内容，可能不需要使用任何工具

### 第 3 步：生成提示词

**重要：如果第 1 步判断为"简单章节型"，直接使用第 1.5 步的简化流程生成提示词，不套用复杂模板。**

对于非简单章节，读取 `references/prompt-templates.md`，根据内容类型选择对应模板，然后填充以下内容：

#### 3.1 章节基本信息
- 章节号和标题：直接从例文提取
- 标题层级：根据章节号计算（参考规则 1 的映射表）
- 子节结构：如果例文包含子节，逐一列出每个子节的标题和内容范围

#### 3.2 写作规则注入（重点）

将从例文中提取的写作规则写入提示词，格式如下：

**正文写作规律：**

分析例文的段落结构，按以下方式组织：

1. 按表格/主题组织段落（不是按单个指标拆分）：
   - 如果例文中同一表格的数据在一个段落内，提示词也应该合并为一个段落
   - 如果例文中不同表格的数据分开，提示词也应该分开
   - 段落内的不同指标使用逗号分隔，不要使用空行

2. 每个表格/主题的写作规律：
   - [表格/主题名称]：
     * 包含的指标：[列出该段落包含的所有指标]
     * 写作格式：[整体句式结构，使用通用占位符]
     * 表格绑定字段：[从表格中提取哪些字段]
     * 通用格式模板：[写成完整段落格式，指标之间用逗号分隔]

3. 数据来源：
   - [字段名称]：从表格[行/列定义]中提取
   - ...

#### 3.3 表格绑定内容的通用化处理

对于从例文中识别的表格绑定内容，必须进行通用化处理：

**原始例文内容（具体数据）：**
```
金妥昔单抗的平均暴露持续时间为22.21周（SD：22.379）
```

**通用化后的提示词内容：**
```
[药物A]的平均暴露持续时间为[数值]周（SD：[数值]）
```

**通用化规则：**
1. 将具体药物名称替换为 [药物A]、[药物B] 等占位符
2. 将具体数值替换为 [数值] 占位符
3. 将具体分组名称替换为 [试验组]、[对照组] 等占位符
4. 保留固定的描述格式和句式结构
5. 确保模板能应对不同数量的药物/分组

#### 3.4 研究特定信息占位符
如果例文中出现具体研究信息（药名、瘤种、剂量等），在提示词中使用抽象占位符：
- 研究药物/对照药/基础治疗/联合治疗
- 目标适应症或研究人群
- 主要终点/次要终点/安全性终点

同时在当前研究检索重点中保留具体关键词作为示例。

#### 3.5 通用规则注入
从 `references/common-rules.md` 读取通用规则，注入到提示词中。通用规则包括：
- CSR 正文风格约束
- 禁止事项
- 格式规则
- 校验规则
- 输出规则

#### 3.6 原文复制规则（定义型内容专用）

对于定义型内容（从 CSP/SAP 提取原文），在提示词中注入以下规则：

**原文保留规则**：
- 若例文要求逐字保留定义性内容，应尽量以原文为基础进行最小必要整合，避免改写关键定义
- 保留原始措辞、顺序、标点、编号、标签
- 除非模板明确要求增加包装文本、标题、占位符替换，否则不进行改写、总结、转述、术语规范化或医学语言润色

**引用保留规则**：
- 如果原文使用"可依据""详见""可参考""参考"后跟章节号或附件号，则只保留引用性表述本身，不要展开被引用章节或附件的具体内容
- CSP/SAP 中的内部章节引用（如"见6.2章节""详见附件16.1.1"）不能直接带入 CSR 正文，应删除此类引用或替换为 CSR 中对应的章节编号

**内容过滤规则**：
- 只纳入与当前章节标题直接对应的原文段落
- 如果片段中出现其他章节的标题，则该标题下的内容不属于本节，不要纳入
- 允许在同一主题下整合多个原文片段形成连续论述，但新增句子只能用于连接原文逻辑，不能引入方案中未出现的新依据、新结论

**目标效果处理规则（定义型内容专用）**：
- 对于定义型内容（第 9 章等从 CSP/SAP 提取原文的章节），不提供目标效果正文
- 在"目标效果"部分写明：本章节内容完全从CSP/SAP中"{章节标题}"对应章节检索誊写，不提供目标效果正文。
- 目的：防止 Agent 不检索直接输出提示词中的内容，强制从文档中检索原文

#### 3.7 动态子章节生成（如适用）

如果第 1.5 步识别出需要动态生成子章节，在提示词中注入以下规则：

**A. TFL 数据驱动型动态子章节**

适用于子章节由 TFL 表格文件名决定的场景（如 11.4.1、12.5）：

```
子章节生成规则：
1. 子章节数量根据实际检索到的表格动态确定
2. 子章节名称从表格文件名中提取，不要预先固定
3. 每个子章节使用通用的写作规律模板

对于每个读取到的表格，按照以下规律生成对应的子章节：

[从例文提取的子章节写作规律]

重复以上步骤，为每个读取的表格生成对应的子章节。

表格类型写作规律（如适用）：
- [表格类型1]：[写作规律]
- [表格类型2]：[写作规律]
- ...
```

**B. CSP/SAP 内容驱动型动态子章节**

适用于子章节需要从 CSP/SAP 文档中提取的场景（如 11.4.2）：

```
子章节生成规则：
1. 子章节数量根据 CSP/SAP 文档内容动态确定
2. 子章节主题从文档中提取，不要预先固定
3. 每个子章节使用通用的写作规律模板

执行步骤：
第一步：读取 CSP/SAP 文档
调用 Tools -> read_document，搜索与本章节相关的内容。
- documentPath：CSP/SAP 文件路径
- requirement："{从例文提取的检索主题}"

第二步：识别子章节主题
从 read_document 返回的内容中，识别需要讨论的子主题：
- 提取文档中与本章节相关的主题列表
- 每个主题对应一个子章节
- 不要预先固定子章节名称

第三步：为每个子主题生成子章节
对于每个识别出的子主题，按照以下规律生成对应的子章节：

[从例文提取的子章节写作规律]

重复以上步骤，为每个子主题生成对应的子章节。
```

**C. 混合型动态子章节**

适用于部分子章节由 TFL 数据决定、部分由 CSP/SAP 内容决定的场景：

```
子章节生成规则：
1. TFL 数据驱动的子章节：根据表格文件名自动生成
2. CSP/SAP 内容驱动的子章节：从文档中提取主题后生成
3. 两种类型的子章节可以合并输出

执行步骤：
第一步：生成 TFL 数据驱动的子章节
[按 TFL 数据驱动型规则执行]

第二步：生成 CSP/SAP 内容驱动的子章节
[按 CSP/SAP 内容驱动型规则执行]

第三步：整合输出
将两种类型的子章节按逻辑顺序整合为完整章节。
```

#### 3.8 简单章节提示词生成（简单章节型专用）

对于简单章节，使用以下简化流程生成提示词：

**A. 分析例文确定输出模式**：

1. **引用表格模式**：如果例文包含"见表 X.X"、"详见附表"等引用
   - 需要使用 `read_tfl` 尝试检索
   - 输出格式：找到表格时输出参考信息，未找到时输出固定内容

2. **引用章节模式**：如果例文包含"见 X.X 章节"、"详见第 X 章"等引用
   - 需要使用 `read_document` 检索
   - 输出格式：找到内容时输出参考信息，未找到时输出固定内容

3. **固定输出模式**：如果例文没有引用任何内容
   - 可能不需要使用任何工具
   - 直接输出固定格式的内容

**B. 生成简单提示词结构**：

```markdown
# {章节号} {章节标题}

## 任务描述
根据检索结果撰写本章节正文。

## 检索策略
1. 使用 {工具名称} 检索 {检索关键词}
2. 如果找到相关数据，输出参考信息
3. 如果未找到相关数据，输出固定内容

## 输出格式
### 情况一：找到相关数据
{根据例文分析的输出格式}

### 情况二：未找到相关数据
{固定输出内容}

## 输出要求
- 保持专业、客观的语气
- 遵循 CSR 正文风格
- 不要添加例文中没有的内容
```

**C. 简单章节示例分析**：

以 11.4.3 个体疗效数据列表为例：
- 例文："个体疗效数据列表见表 11.4.3-01"
- 分析：引用了表格，需要使用 `read_tfl`
- 输出模式：找到表格时输出参考信息，未找到时输出固定内容

以 11.4.4 药物-药物和药物-疾病相互作用为例：
- 例文："药物-药物和药物-疾病相互作用见表 11.4.4-01"
- 分析：引用了表格，需要使用 `read_tfl`
- 输出模式：找到表格时输出参考信息，未找到时输出固定内容

**D. 注意事项**：
1. 不要为简单章节套用复杂模板（如模板 A-H）
2. 不要添加例文中没有的写作规律或段落结构
3. 保持提示词简洁明了
4. 确保输出格式与例文一致

**D. 动态子章节的通用要求**

1. 子章节划分根据实际数据自动识别，不要预先固定子章节名称
2. 如果某类表格/主题不存在，对应的子章节应被删除
3. 不要编造不存在的数据或主题
4. 每个子章节最后输出该子章节的表格引用和占位符（如适用）

### 第 4 步：输出提示词文件

将生成的提示词保存为 .md 文件，文件名格式：{章节号}{章节标题}.md

同时输出一份简要说明，包括：
- 识别的内容类型
- 使用的工具配置
- 提取的写作规则摘要
- 表格绑定内容的通用化处理结果
- 需要用户确认或调整的地方

## 用户自定义选项

用户可以在提供例文时附加以下自定义要求：

### 写作规则自定义

用户可以说：
- 不要使用 $TABLE 占位符，直接引用表格编号
- 目标效果中保留具体药名作为参考
- 增加关于统计方法的详细描述要求
- 不要包含个体数据引用部分

### 风格自定义

用户可以说：
- 风格更简洁，每段不超过 3 句话
- 参考 XX 公司的 CSR 风格
- 增加关于不良事件归因的描述要求
- 使用更正式的学术写作风格

### 工具配置自定义

用户可以说：
- 这个章节不需要 read_tfl，只用 read_document
- 增加 read_figure 工具用于读取流程图
- 使用 read_document 读取 CSP 中的入排标准

## 输出格式

生成的提示词必须遵循以下结构（根据内容类型和例文数据来源类型选择对应模板）。注意：模板中所有 `{HEADING}` 占位符需替换为根据章节号计算出的正确标题层级。

### 数据摘要型模板结构（TFL 数据驱动）

根据例文的数据来源类型，生成的提示词结构有所不同：

#### 类型 A：例文包含实际表格数据

当例文包含完整的表格数据（如表37、表38的行列内容）时，生成包含占位符和交叉引用规则的完整提示词：

```
任务目标：
基于指定 TFL 表格自动生成 CSR 的[{章节标题}]章节，并输出相关引用及原始表格。

可用工具：
1. Tools -> read_tfl
用途：读取指定 TFL 表格内容。
参数：
- folderPath（必填）：TFL表格文件所在目录路径（见全局配置）
- requirement 或 fileNameHint：查询需求或文件名提示
返回格式：
1、原始Html
2、表格Markdown结果
3、表格的唯一码，用于后续调用
4、表格文件名

2. Tools -> link_tableslisting
用途：根据 read_tfl 返回结果或主表文件路径和列表目录，按文件名匹配对应列表文件，生成引用语句。
参数：
- folderPath（必填）：Listing文件所在目录路径（见全局配置）
- requirement：查询需求描述

执行步骤：
第一步：检索 TFL 表格
调用 Tools -> read_tfl，搜索以下内容的表格（使用requirement参数进行内容匹配）：
- requirement: "{从例文提取的检索关键词}"

注意：
- 使用requirement参数进行搜索，不要使用fileNameHint
- 搜索关键词应基于本章节的数据需求描述，而非具体表名
- 如果搜索结果不匹配，可以调整requirement关键词重新搜索
- 如果检索不到匹配的表格，对应的正文段落应被删除

第二步：分析表格结构，识别数据字段
仔细分析读取到的表格 Markdown 内容，识别以下信息：
1. 表格的列定义（各分组名称、合计列）
2. 表格的行定义（各项统计指标）
3. 表格脚注中的特殊说明

从表格中自动识别并提取数据（根据表格实际内容）：
- {从例文分析出的数据类别，如：药物暴露持续时间、剂量强度等}
- 不要预设具体的药物名称或分组名称，根据表格列定义自动识别
- 不要预设具体的字段名称，根据表格行定义自动识别

第三步：生成[{章节标题}]章节正文

动态段落生成规则：
- 根据实际检索到的表格数量和内容动态生成段落
- 如果某个表格检索不到，对应段落应被删除
- 不要编造不存在的数据
- 根据表格实际包含的字段动态生成描述（不预设字段名称）

正文写作规律：
{从例文提取的写作规则，包括：}
1. 整个章节分为 X 个段落：
   - 第一段：[段落主题]
   - 第二段：[段落主题]
   - ...

2. 每个段落的写作规律：
   - 第一段：
     * 描述内容：[这段要描述什么]
     * 写作格式：[具体的句式结构，使用通用占位符]
     * 表格绑定字段：[从表格中提取哪些字段]
   - 第二段：
     * ...

3. 数据来源：
   - [字段名称]：从表格[行/列定义]中提取
   - ...

第四步：输出 TFL 数据集引用
调用 Tools -> link_tableslisting，搜索与本章节相关的表格的个体数据引用信息。
搜索关键词应基于本章节实际检索到的表格内容。

输出要求：
- 使用表格/图片文件名中的编号（如表14.1.8.1），不使用$TABLE/$IMAGE占位符
- 先列出表格引用信息，再列出个体数据引用信息

第五步：输出原始表格占位符
不要直接展开原始表格 HTML，也不要自行转写表格内容。
规则：
- 正文末尾只插入提供详细数据的表格占位符
- 必须原样输出 read_tfl 返回的 tablePlaceholder/table_placeholder
- 输出占位符前，先输出表格标题，格式为：表* xxx（xxx为表格文件名中的名称）
- 表格标题和占位符要换行写
- 只输出实际检索到的表格的占位符

数据校验规则：
{从 common-rules.md 注入的规则}

异常处理规则：
{从 common-rules.md 注入的规则}

统一写作规则：
{从 common-rules.md 注入的规则，包含占位符交叉引用规则}

最终输出格式：
1. 仅输出[{章节标题}]章节正文
2. 如正文确需插入原始表格，仅在正文相应位置输出占位符

最终输出补充要求：
1. 最终结果必须是一个完整 Markdown 文本。
2. 若需要插入原始表格，必须输出工具返回的 placeholder，不得改写占位符文本。
3. 程序会在最后一步执行完成后，将 Markdown 转为 HTML，并用 placeholder 替换为实际 HTML 表格内容。
4. 不要输出类似"TFL 数据集引用""原始表格 HTML 路径""原始表格"之类的标题。
```

#### 类型 B：例文仅包含表格引用和附件引用

当例文只包含表格引用信息（如"详见表14.3.6.1"）和附件引用信息（如"详见附件列表16.2.9.1"），不包含实际表格数据时，生成不包含占位符的简化提示词：

```
任务目标：
基于指定 TFL 表格自动生成 CSR 的[{章节标题}]章节。

可用工具：
1. Tools -> read_tfl
用途：读取指定 TFL 表格内容。
参数：
- folderPath（必填）：TFL表格文件所在目录路径（见全局配置）
- requirement 或 fileNameHint：查询需求或文件名提示
返回格式：
1、原始Html
2、表格Markdown结果
3、表格的唯一码，用于后续调用
4、表格文件名

2. Tools -> link_tableslisting
用途：根据 read_tfl 返回结果或主表文件路径和列表目录，按文件名匹配对应列表文件，生成引用语句。
参数：
- folderPath（必填）：Listing文件所在目录路径（见全局配置）
- requirement：查询需求描述

执行步骤：
第一步：检索 TFL 表格
调用 Tools -> read_tfl，搜索以下内容的表格（使用requirement参数进行内容匹配）：
- requirement: "{从例文提取的检索关键词}"

注意：
- 使用requirement参数进行搜索，不要使用fileNameHint
- 搜索关键词应基于本章节的数据需求描述，而非具体表名
- 如果搜索结果不匹配，可以调整requirement关键词重新搜索
- 如果检索不到匹配的表格，对应的正文段落应被删除

第二步：分析表格结构，识别数据字段
仔细分析读取到的表格 Markdown 内容，识别以下信息：
1. 表格的列定义（各分组名称、合计列）
2. 表格的行定义（各项统计指标）
3. 表格脚注中的特殊说明

从表格中自动识别并提取数据（根据表格实际内容）：
- {从例文分析出的数据类别}
- 不要预设具体的名称，根据表格定义自动识别

第三步：生成[{章节标题}]章节正文

动态段落生成规则：
- 根据实际检索到的表格数量和内容动态生成段落
- 如果某个表格检索不到，对应段落应被删除
- 不要编造不存在的数据
- 根据表格实际包含的字段动态生成描述（不预设字段名称）

正文写作规律：
{从例文提取的写作规则}

第四步：输出引用信息
调用 Tools -> link_tableslisting，搜索与本章节相关的表格和附件的引用信息。

输出要求：
- 使用表格/图片文件名中的编号（如表14.3.6.1、列表16.2.9.1）
- 按照例文中的引用格式输出

数据校验规则：
{从 common-rules.md 注入的规则}

异常处理规则：
{从 common-rules.md 注入的规则}

统一写作规则：
{从 common-rules.md 注入的规则，不包含占位符交叉引用规则}

最终输出格式：
1. 仅输出[{章节标题}]章节正文

最终输出补充要求：
1. 最终结果必须是一个完整 Markdown 文本。
2. 不要输出类似"TFL 数据集引用""原始表格 HTML 路径""原始表格"之类的标题。
```

### 定义型模板结构（CSP/SAP 驱动）

```
你是一名负责撰写临床试验 CSR 正文的专业医学写作 agent。

请完成以下任务：

1. 目标章节
请撰写 CSR 正文中的以下小节，仅输出该小节正文内容：

{HEADING}
[{章节标题}]()

2. 信息来源与工具要求
本任务中，"{章节标题}"属于方案定义性内容，不允许凭经验补写，必须优先从指定的 CSP / Protocol / SAP 原文中提取可追溯内容。

如需提取方案定义，请优先调用 read_document，并遵循以下要求：
- documentPath 传入对应的 CSP、Protocol 或 SAP 文件路径
- requirement 应明确写为"{从例文提取的检索主题}"
- 如有必要，可补充 query 或 keywords，例如：{从例文提取的关键词列表}
- 优先使用 read_document 返回的 extractedPassages / extractedPassageItems 中可追溯的原文内容
- 同时检查 read_document 返回的 fragments 中所有片段的 content，确保没有遗漏

3. 写作要求
{从 common-rules.md 注入的规则}

4. 输出格式要求
最终仅输出以下格式：

{HEADING}

随后直接输出正文。

5. 目标效果
目标输出风格参考如下，仅用于约束标题层级、段落密度和CSR文风，不代表可直接复用其中的研究事实：

{从例文提取的目标效果，去除具体研究事实}

6. 当前任务
现在请基于指定文档中的原文定义，生成该小节的最终正文。
```

## 质量检查清单

生成的提示词必须包含以下质量检查要求（根据内容类型选择适用项）：

### 通用质量检查

1. **固定字段完整性**：标题、标签、表头、固定句式等模板锁定的内容是否完整保留
2. **变量字段来源**：变量字段（名称、日期、数值、单位等）是否来自当前输入或已使用占位符标记
3. **输出格式一致性**：输出格式是否与锁定格式完全一致
4. **内容边界**：是否混入相邻章节内容
5. **原文忠实度**：verbatim source copy 内容块是否与来源文本完全一致

### 定义型内容检查

6. **来源追溯**：内容是否可追溯到 CSP/SAP 原文
7. **未编造内容**：是否引入了方案中未定义的新信息
8. **引用处理**：CSP/SAP 内部引用是否已删除或替换为 CSR 章节编号

### 数据型内容检查

9. **表格绑定准确**：表格绑定字段是否正确对应表格中的行/列定义
10. **占位符完整**：如使用占位符，正文引用与占位符数量是否匹配
11. **数据校验**：数值、百分比、例数是否与表格数据一致

### 检查清单注入方式

在生成的提示词末尾，根据内容类型添加对应的检查项。例如对于定义型内容：

```
输出校验规则：生成正文后，自行检查以下内容：
1）所有子节是否都已输出，有无遗漏
2）编号列表格式是否与原文一致
3）引用性表述是否保持原样
4）是否有 CSP/SAP 中不存在的内容被编造
5）正文中是否包含"未检索到""无法回答""根据工具结果"等说明文字，如有必须删除
```

对于数据型内容：

```
数据校验规则：
1）表格绑定字段是否正确对应表格中的行/列定义
2）正文引用与占位符数量是否匹配
3）数值、百分比、例数是否与表格数据一致
4）是否有编造的数据或结论
```

## 注意事项

1. 写作规则是核心：重点从例文中提取写作规则和描述方法，而不是段落结构
2. 表格绑定内容必须通用化：将具体数据替换为通用占位符，确保提示词能应对不同数据
3. 不要过度拟合例文：目标效果应保留文风参考价值，但去除具体研究事实
4. 关键词要全面：从例文中提取所有可能的检索关键词，包括同义词和缩写
5. 子节要完整：如果例文包含子节，每个子节都必须在提示词中体现
6. 工具配置要准确：根据例文实际引用的数据源选择工具，不要遗漏
7. 标题层级要准确：根据章节号的层级结构计算正确的 Markdown 标题层级
